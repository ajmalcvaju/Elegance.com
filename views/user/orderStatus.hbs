<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Order Status</title>
    <style>
        /* Style the table */
        .table {
            width: 80%;
            border-collapse: collapse;
            margin-bottom: 20px;
            margin-left: 10%;
            margin-right: 10%;
            font-family: system-ui, -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Oxygen, Ubuntu, Cantarell, 'Open Sans', 'Helvetica Neue', sans-serif;
        }

        /* Style the table header */
        .table th {
            background-color: #333;
            color: #fff;
            font-weight: bold;
            text-align: left;
            padding: 10px;
        }

        /* Style the table body */
        .table td, .table th {
            border: 1px solid #ddd;
            padding: 8px;
        }

        /* Add a background color to even rows */
        .table tbody tr:nth-child(even) {
            background-color: #f2f2f2;
        }

        /* Make the table responsive */
        @media (max-width: 768px) {
            .table {
                overflow-x: auto;
            }
        }
    </style>
</head>
<body>
    <div style="overflow-x:auto;">
        <h1 style="text-align: center;">Order Status</h1>
        <table class="table">
            <thead class="thead-light">
                <tr>
                    <th scope="col">Order Id</th>
                    <th scope="col">Date</th>
                    <th scope="col">Products</th>
                    <th scope="col">Quantity</th>
                    <th scope="col">Price</th>
                    <th scope="col">Status</th>
                    <th scope="col">Expected Arrival</th>
                    <th scope="col">Payment Method</th>
                    <th scope="col">Cancell</th>
                </tr>
            </thead>
            <tbody>
                {{#each order}}
          {{#each this.items}}
                <tr>
                    <td>1</td>
                    <td>{{this.date}}</td>
                    <td>{{this.productId.pname}}</td>
                    <td>{{this.quantity}}</td>
                    <td>{{this.price}}</td>
                    <td>{{this.status}}</td>
                    <td>{{this.expectedArrival}}</td>
                    <td>{{this.paymentMethod}}</td>
                    <td><a href="/cancelOrder?orderId={{../_id}}&productId={{this.productId._id}}" class="btn btn-danger"  onclick="return confirm('Do You want to Cancell {{this.productId.pname}}?')">Cancell</a></td>

                </tr>
                <tr>
                {{/each}}
          {{/each}}
                   
            </tbody>
        </table>
    </div>
</body>
</html>
